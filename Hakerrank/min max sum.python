def miniMaxSum(arr):
    tot_sum = arr[0]
    min_val = arr[0]
    max_val = arr[0]
    
    for val in arr[1:]:
        tot_sum += val
        min_val = min(val, min_val)
        max_val = max(val, max_val)
        
    print(tot_sum - max_val, tot_sum - min_val)
